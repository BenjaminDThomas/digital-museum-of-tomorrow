{
  "name": "digital-museum-of-tomorrow",
  "version": "1.0.0",
  "description": "Unit and integration tests for the A2BC digital museum AI features",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jest-environment-jsdom",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
